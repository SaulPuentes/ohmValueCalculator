const { WeatherAPI } = require("../src/weather");

// Create a tests script to validate: 
// Valid response object 
// Required parameters 
// Possible API errors 


test('two plus two is four', () => {
  expect(2 + 2).toBe(4);
});